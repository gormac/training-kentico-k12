using System.Threading.Tasks;
using Microsoft.AspNet.Identity;

using Business.Identity.Models;

namespace Business.Identity
{
    public interface IMedioClinicUserManager
    {
        //Task<IdentityResult> UpdatePassword(IUserPasswordStore<MedioClinicUser, int> passwordStore, MedioClinicUser user, string newPassword);
        //Task<bool> VerifyPasswordAsync(IUserPasswordStore<MedioClinicUser, int> store, MedioClinicUser user, string password);
    }
}